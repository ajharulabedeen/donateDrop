import { PostcommentWithUserInfo } from './postcomment-with-user-info.model';

describe('PostcommentWithUserInfo', () => {
  it('should create an instance', () => {
    expect(new PostcommentWithUserInfo()).toBeTruthy();
  });
});
