import { UserPublicContact } from './user-public-contact.model';

describe('UserPublicContact', () => {
  it('should create an instance', () => {
    expect(new UserPublicContact()).toBeTruthy();
  });
});
