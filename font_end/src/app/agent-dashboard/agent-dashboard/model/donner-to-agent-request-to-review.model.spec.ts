import { DonnerToAgentRequestToReview } from './donner-to-agent-request-to-review.model';

describe('DonnerToAgentRequestToReview', () => {
  it('should create an instance', () => {
    expect(new DonnerToAgentRequestToReview()).toBeTruthy();
  });
});
