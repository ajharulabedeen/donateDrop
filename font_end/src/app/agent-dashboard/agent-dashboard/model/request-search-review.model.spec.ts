import { RequestSearchReview } from './request-search-review.model';

describe('RequestSearchReview', () => {
  it('should create an instance', () => {
    expect(new RequestSearchReview()).toBeTruthy();
  });
});
