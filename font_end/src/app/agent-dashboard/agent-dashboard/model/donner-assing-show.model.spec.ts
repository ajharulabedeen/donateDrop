import { DonnerAssingShow } from './donner-assing-show.model';

describe('DonnerAssingShow', () => {
  it('should create an instance', () => {
    expect(new DonnerAssingShow()).toBeTruthy();
  });
});
