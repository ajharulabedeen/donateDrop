import { RequestGetAgentRequests } from './request-get-agent-requests.model';

describe('RequestGetAgentRequests', () => {
  it('should create an instance', () => {
    expect(new RequestGetAgentRequests()).toBeTruthy();
  });
});
