import { RequestReviewRequest } from './request-review-request.model';

describe('RequestReviewRequest', () => {
  it('should create an instance', () => {
    expect(new RequestReviewRequest()).toBeTruthy();
  });
});
