import { AgentRequestToReview } from './agent-request-to-review.model';

describe('AgentRequestToReview', () => {
  it('should create an instance', () => {
    expect(new AgentRequestToReview()).toBeTruthy();
  });
});
