import { RequestPersonalNote } from './request-personal-note.model';

describe('RequestPersonalNote', () => {
  it('should create an instance', () => {
    expect(new RequestPersonalNote()).toBeTruthy();
  });
});
